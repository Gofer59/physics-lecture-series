When we compute cross sections involving unpolarized particles, we need to sum over spins. The spin-summed outer products have beautiful closed forms. Sum over s of u times u-bar equals p-slash plus m. Sum over s of v times v-bar equals p-slash minus m.

These two matrix identities are the workhorse of every QED calculation. They collapse traces of spinor products into traces of gamma matrices — which is the next lecture's favourite computation.